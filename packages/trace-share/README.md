# @frumu/trace-share

npm wrapper package for the `trace-share` CLI binary.

## Install

```bash
npm i -g @frumu/trace-share
```

## Usage

```bash
trace-share --help
```

The package installs a platform-specific binary from GitHub Release assets.

Project docs and proposal:

- https://github.com/frumu-ai/trace-share
